//User profile page 

$(document).ready(function(){

    showBucketlists(bucketlists)

    for (const bucketlist in bucketlists){

    };

    $('#save-order-btn').on('submit', (evt) =>{
        evt.preventDefault();
        
    });

//function that shows all of the user's bucketlists
//for bucketlist in bucetlists show every bucketlist that a user has 
});

